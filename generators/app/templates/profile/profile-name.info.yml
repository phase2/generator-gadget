name: <%= projectName %>
core: 8.x
type: profile
description: '<%= projectDescription %>'
config_install: true
base profile:
  name: lightning
  excluded_dependencies:
    # sequential list of modules from Lightning you don't want enabled
dependencies:
  # sequential list of additional modules to enable
  - drupal:paragraphs
