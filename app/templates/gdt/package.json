{
  "name": "grunt-drupal-tasks-example",
  "description": "Example package.json for a Drupal project built with grunt-drupal-tasks.",
  "version": "0.1.0",
  "engines": {
    "node": ">=0.12.0",
    "npm": "2.x"
  },
  "private": true,
  "scripts": {
    "start": "grunt --timer",
    "stop": "grunt clean:default",
    "test": "grunt validate && grunt test"
  },
  "dependencies": {
    "grunt": "^1.0.0",
    "grunt-drupal-tasks": "^1.0.0-alpha",
    "zombie": "^2.5.1"
  }
}
